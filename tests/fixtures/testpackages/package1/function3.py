# depend on package that doesn't exist

import pkg_doesnt_exist

def function_uses_external_package_not_installed(df):
    """
    Doesn't do anything - just tests that there are clean
    reactions to the package not being there
    """
    pass